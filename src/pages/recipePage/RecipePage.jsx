import "../recipePage/RecipePage.css";
import { useContext, useEffect, useState, useCallback } from "react";

const RecipeScreen = () => {
  const [recipes, setRecipes] = useState([]);

  return (
    <>
      <p>test</p>
      <button>Add new recipe if no recipes exist</button>
    </>
  );
};

export default RecipeScreen;
